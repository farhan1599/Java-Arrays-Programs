package com.practice.arrays;

public class Sort {

	public static void main(String[] args) {

		int[] arr = { 23, 45, 12, 56, 78 };

		sortTheArray(arr);
	}

	private static void sortTheArray(int[] arr) {

		for (int i = 0; i < arr.length - 1; i++) {

			for (int j = i + 1; j < arr.length; j++) {

				if(arr[i] > arr[j]) {
						
				}
			}
		}

	}
}
